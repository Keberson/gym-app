import { ScrollView, Text } from "react-native";

const GeneralScreen = () => {
    return (
        <ScrollView>
            <Text>GeneralScreen</Text>
        </ScrollView>
    );
};

export default GeneralScreen;
