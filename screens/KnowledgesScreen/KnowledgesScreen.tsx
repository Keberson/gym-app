import { ScrollView, Text } from "react-native";

const KnowledgesScreen = () => {
    return (
        <ScrollView>
            <Text>KnowledgesScreen</Text>
        </ScrollView>
    );
};

export default KnowledgesScreen;
